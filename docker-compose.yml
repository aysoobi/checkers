services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: checkers
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - checkers_pg_data:/var/lib/postgresql/data

volumes:
  checkers_pg_data:
